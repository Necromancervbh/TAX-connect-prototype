<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    
    <!-- Permissions for Camera and Audio (Agora & Cloudinary) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    
    <!-- Required for Call Notifications to wake screen / pop up -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    
    <!-- Declare features as not required to support ChromeOS and large screens -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    
    <!-- Storage Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Calendar Permissions -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="${RAZORPAY_API_KEY}" />
        
        <activity
            android:name=".features.auth.LoginActivity"
            android:exported="true" />

        <activity android:name=".features.auth.RegisterActivity" />
        <activity
            android:name=".features.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".features.home.MainActivity"
            android:exported="true" />
        <activity
            android:name=".features.home.HomeActivity"
            android:label="Home" />
        <activity
            android:name=".features.dashboard.CADashboardActivity"
            android:label="Home" />
        <activity android:name=".features.chat.ChatActivity" 
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop"/>
        <activity android:name=".features.profile.ProfileActivity" />
        <activity
            android:name=".features.videocall.VideoCallActivity"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation" />
        <activity android:name=".features.ca.CADetailActivity" />
        <activity android:name=".features.chat.MyChatsActivity" />
        <activity android:name=".features.booking.OrderHistoryActivity" />

        <activity android:name=".features.booking.BookAppointmentActivity" />
        <activity android:name=".features.booking.AvailabilitySettingsActivity" />

        <activity android:name=".features.wallet.WalletActivity" />
        <activity android:name=".features.documents.BalanceSheetActivity" />
        <activity android:name=".features.booking.MyBookingsActivity" />
        <activity android:name=".features.documents.MyDocumentsActivity" />
        <activity android:name=".features.ca.ExploreCAsActivity" />
        <activity android:name=".features.community.CommunityActivity" />
        <activity android:name=".features.community.PostDetailActivity" />
        <activity android:name=".features.milestones.MilestonesActivity" />
        <activity android:name=".features.notification.NotificationSettingsActivity" />
        <activity android:name=".features.notification.NotificationHistoryActivity" />
        <activity android:name=".features.documents.SecureDocViewerActivity" 
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".features.videocall.VideoPlayerActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"/>

        <service
            android:name=".data.services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver android:name=".data.services.CallActionReceiver" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
